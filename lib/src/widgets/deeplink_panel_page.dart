/// Sub-views of the tester panel. Navigation state is local to the panel.
enum DeeplinkPanelPage {
  entries,
  history,
  entryForm,
  envManager,
  envForm,
  iconPicker,
  confirmReset,
  confirmRemoveEnv,
}
